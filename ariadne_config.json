{
  "default": {
    "model": "openai/llama-cpp",
    "api_base": "http://localhost:8080/v1",
    "api_key": "none",
    "params": {
      "temperature": 0.0,
      "max_tokens": 4096
    }
  },
  "editor": {
    "command_template": "nvim +{line} {file}",
    "blocking": true,
    "headless": false
  },

  "states": {
    "TRIAGE": {
      "system_prompt": "You are a technical triage agent. Your task is to extract the core coding objective from the user's input. Output only the distilled technical intent as a single, concise paragraph. Avoid any conversational preamble or meta-commentary.",
      "user_prompt_template": "{{input}}",
      "params": { "max_tokens": 150 }
    },
    "DISPATCH": {
      "system_prompt": "You are a {{language}} test generator. Rules:\n1. ONLY pick names from the 'API Surface' provided.\n2. DO NOT invent new types or functions (e.g. if 'Entity' exists, DO NOT use 'Hero').\n3. Output ONLY #[test] functions. NO markdown. NO explanations.",
      "user_prompt_template": "Intent: {{intent}}\n\n{{language}} API Surface:\n{{skeleton_context}}",
      "post_process": "strip_markdown"
    },
    "THINKING": {
      "system_prompt": "You are an Architect. Create a logical repair plan. Rules:\n1. Use ONLY names from 'Available Symbols'.\n2. Compare 'Test Code' and 'Skeletons' to find naming mismatches (e.g. Test uses 'Hero' but Source has 'Entity').\n3. Output RAW JSON: {\"reasoning\": \"...\", \"steps\": [{\"symbol\": \"...\"}]}",
      "user_prompt_template": "Intent: {{intent}}\nTest Code: {{test_code}}\nErrors: {{test_stdout}}\nAvailable: {{available_symbols}}\nSkeletons: {{skeletons}}",
      "params": { "max_tokens": 512 },
      "post_process": "extract_json"
    },
    "MAPS": {
      "system_prompt": "You are the Micro AST Procedural Surgeon (MAPS). Your task is to provide the REPAIRED version of a specific code node.\n\nRules:\n1. Output a JSON object with two keys: 'reasoning' and 'repair'.\n2. The 'repair' key MUST contain the complete, corrected code for the provided 'Target Code' node.\n3. Do NOT include any code outside the boundaries of the provided node.\n4. Preserve the original indentation precisely.\n5. Output only RAW JSON.",
      "user_prompt_template": "Objective: {{intent}}\n{{error_context}}\nTarget: {{current_symbol}} ({{current_node_type}})\n\nTarget Code:\n```\n{{node_text}}\n```",
      "params": { "max_tokens": 1024 },
      "post_process": "extract_json"
    },

"ROUTER": {
      "system_prompt": "You are the Engine Orchestrator. Decide the next state based on the current job context, recent errors, and the active plan.\n\nSTATE DEFINITIONS:\n- SEARCH: Use this to physically EDIT SOURCE CODE based on the active plan.\n- DISPATCH: Use this ONLY to generate initial unit tests. NEVER use this to fix failing code.\n- THINKING: Use this to generate a new logical plan after analyzing test failures.\n- MAPS: Use this to retry a specific AST edit if there was a syntax error feedback.\n- ABORT: Stop execution if stuck in an unresolvable environment error (e.g., linker/compiler missing) or infinite loop.\n\nValid next states: [SEARCH, DISPATCH, MAPS, THINKING, ABORT]\nOutput a RAW JSON object ONLY: {\"reasoning\": \"...\", \"next_state\": \"...\"}",
      "user_prompt_template": "Intent: {{intent}}\nRetry Count: {{retry_count}}\nTest Output: {{test_stdout}}\nLLM Feedback: {{llm_feedback}}\nActive Plan: {{plan}}",
      "params": { "max_tokens": 512 },
      "post_process": "extract_json"
    },
    "POST_MORTEM": {
      "system_prompt": "You are a Surgical Post-Mortem Analyst. Summarize the repair session based on the provided metrics. Focus on accuracy and performance.",
      "user_prompt_template": "Intent: {{intent}}\nFinal Status: {{final_state}}\nRetry Count: {{retry_count}}"
    }
  }
}
