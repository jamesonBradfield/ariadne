{
  "default": {
    "model": "openai/llama-cpp",
    "api_base": "http://localhost:8080/v1",
    "api_key": "none",
    "params": {
      "temperature": 0.0,
      "max_tokens": 4096
    }
  },
  "states": {
    "TRIAGE": {
      "system_prompt": "You are a triage agent. Output ONLY the technical intent. NO yapping. NO preamble. Output raw text only.",
      "user_prompt_template": "{{input}}",
      "params": { "max_tokens": 50 }
    },
    "DISPATCH": {
      "system_prompt": "You are a {{language}} test generator. Rules:\n1. ONLY pick names from the 'API Surface' provided.\n2. DO NOT invent new types or functions (e.g. if 'Entity' exists, DO NOT use 'Hero').\n3. Output ONLY #[test] functions. NO markdown. NO explanations.",
      "user_prompt_template": "Intent: {{intent}}\n\n{{language}} API Surface:\n{{skeleton_context}}",
      "post_process": "strip_markdown"
    },
    "THINKING": {
      "system_prompt": "You are an Architect. Create a logical repair plan. Rules:\n1. Use ONLY names from 'Available Symbols'.\n2. Compare 'Test Code' and 'Skeletons' to find naming mismatches (e.g. Test uses 'Hero' but Source has 'Entity').\n3. Output RAW JSON: {\"reasoning\": \"...\", \"steps\": [{\"symbol\": \"...\"}]}",
      "user_prompt_template": "Intent: {{intent}}\nTest Code: {{test_code}}\nErrors: {{test_stdout}}\nAvailable: {{available_symbols}}\nSkeletons: {{skeletons}}",
      "params": { "max_tokens": 512 },
      "post_process": "extract_json"
    },
    "CODING": {
      "system_prompt": "You are a surgical execution engine. Output ONLY a SINGLE JSON object with an 'edits' array. Rules:\n1. ONLY rewrite symbols listed in 'Acquired Symbols'.\n2. Fix the specific error mentioned in 'error_context'.\n3. NO markdown. NO explanations. NO yapping.",
      "user_prompt_template": "Intent: {{intent}}\nAcquired Symbols: {{acquired_symbols}}\n{{error_context}}\nCode:\n{{context_str}}",
      "post_process": "extract_json"
    }
  }
}
